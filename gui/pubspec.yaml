name: rolypoly_gui
description: Flutter desktop GUI for the RolyPoly CLI
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flex_color_scheme: ^8.3.0
  desktop_drop: ^0.5.0
  file_selector: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
